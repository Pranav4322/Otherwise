// Set this to your deployed backend's URL once you've deployed backend/
// (e.g. "https://otherwise-api.onrender.com"). Leave it as "" for local
// dev if you're running the backend on the same origin (e.g. via a proxy),
// or point it at "http://localhost:3000" to hit a local backend directly.
window.API_BASE = "";
